CREATE TABLE Player (
    PlayerId INT PRIMARY KEY AUTO_INCREMENT,
    Name NVARCHAR(50) NOT NULL
);

CREATE TABLE Card (
    CardId INT PRIMARY KEY AUTO_INCREMENT,
    Shape TINYINT NOT NULL,
    Fill TINYINT NOT NULL,
    Color TINYINT NOT NULL,
    NrOfShapes TINYINT NOT NULL
);